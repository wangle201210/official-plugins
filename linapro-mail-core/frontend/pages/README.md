# Mail Core frontend pages

- `index.vue` — single-account settings form (route: `linapro-mail-core-settings`).
- Layout matches host/plugin settings pages (`Card` + `Alert` + `Form` + test/save).
- Only one platform mail account is supported on this page for now.
