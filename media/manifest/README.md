# media Manifest

This directory contains install, uninstall, and mock-data assets for the `media` source plugin.

The plugin is Chinese-only for user-facing copy, so no runtime i18n bundles are provided.
