# 总节点概览

```jsonc
//总节点概览（节点列表）
{
  "code": 0,
  "msg": "success",
  "data": {   
      "node_id": "4",                    // 当前节点唯一标识 
      "node_name": "省节点-01",          // 当前节点名称 
      "region": "湖南省",                   // 当前所属区域
      "status": "healthy",                // 当前节点状态：healthy/warning/error/offLine
      "parent_node_id": "0",              // 父节点标识              
      "total_nodes": 8,                     // 总节点数（物理/虚拟节点） 可以从children统计
      "alive_nodes": 7,                    // 存活节点数（心跳正常） 可以从children统计
      "cpu_allocated": 64,            // 当前节点CPU分配核数（ 单位：核）
      "cpu_load": 42.5,              // 当前节点CPU负载（ 单位：核）
      "memory_allocated": 256,        // 当前节点内存分配量（单位：GB）
      "memory_used": 182.3,           // 当前节点内存使用量（单位：GB）
      "disk_io_read": 120,                 // 当前节点磁盘读 MB/s   
      "disk_io_write": 80,                // 当前节点磁盘写 MB/s
      "network_in": 1054.6,                 // 当前节点入流量 Mbps
      "network_out": 3025.5,                // 当前节点出流量 Mbps
      "live_streams": 156,            // 当前节点全局存活直播流数
      "sessions": 2340,               // 当前节点全局会话数（所有实例会话总和）
      "avg_delay": 280,                 // 当前节点全局平均流延迟（单位：ms）
      "last_heartbeat": "2026-05-20 14:30:00",  // 当前节点最后心跳时间 
      "report_time": 1780923543,           // 消息上报时间戳
      "node_latency_map": {                //  当前节点到其他节点的延迟（ms）实例的随机一半去ping加权平均
          "node-02": 18,
          "node-03": 35,
          "node-04": 52
       },
       "child_nodes": [                          // 子节点信息节点列表（支持下钻）结构同父层
           { }
          // 其他节点...
       ]
  }
}
```

# 实例列表

```jsonc
//某个节点详情（实例列表）看是否需要做实例层监控？
{
  "code": 0,
  "msg": "success",
  "data": {
    "node_info": {                         // 节点基础信息（同顶层节点列表字段）
      "node_id": "5",
      "node_name": "怀化节点-01",
      "region": "怀化",
      "status": "healthy"
    },
    "instance_list": [                     // 实例列表（每个实例对应一个进程/容器）
      {
        "instance_id": "inst-001",         // 实例唯一标识（如容器ID）
        "instance_name": "转码实例-01",     // 实例名称（业务标识）
        "status": "running",               // 实例状态：running/stopped/crashed
        "cpu_allocated": 2,                // 实例CPU分配核数（单位：核）
        "cpu_load": 1.3,                   // 实例实时CPU负载（单位：核）
        "memory_allocated": 8,             // 实例内存分配量（单位：GB）
        "memory_used": 5.2,                // 实例内存使用量（单位：GB）
        "disk_io_read": 120,                 // 实例磁盘读 MB/s
        "disk_io_write": 80,                // 实例磁盘写 MB/s
        "network_in": 1054.6,                 // 实例入流量 Mbps
        "network_out": 3025.5,                // 实例出流量 Mbps
        "live_streams": 8,                 // 实例内存活直播流数
        "sessions": 120,                   // 实例内会话数
        "start_time": "2026-05-20 08:15:00",  // 实例启动时间
        "version": "v2.3.1"                // 实例版本号
      }
       // 其他实例...
    ]
  }
}
```
# 流信息列表

```jsonc

//某节点/实例-流信息列表  -支持租户/节点/实例过滤 （前端过滤）
{
  "code": 0,
  "msg": "success",
  "data": {
    "source_type": "node",                 // 数据来源：node（节点）/instance（实例）
    "source_id": "8",                // 来源ID（节点ID或实例ID）
    "stream_list": [                       // 流列表
      {
        "source_url": "xit1Ic2K9MnGd.flv",       // 基础平台流地址
        "stream_id": "stream12345",       // 流唯一标识（UUID）- 可以通过流ID做KILL
        "stream_name": "XX摄像头流",    // 流名称（业务可读  设备编码_通道编码 ） 
        "resolution": "1920x1080",         // 原始分辨率
        "fps": 25,                        // 原始帧率
        "bitrate": 4000,                   // 原始码率（单位：kbps）
        "packet_loss": 0.12,                  // 源流丢包率
        "status": "playing",               // 流状态：playing/paused/error
        "start_time": "2024-05-20 12:00:00",  // 流开始时间
        "duration": 7200,                  // 已运行时长（单位：秒）
        "avg_delay": 220,                  // 源流延迟（单位：ms） 基础到服务器
        "protocol_count": 2,               // 支持的协议数（如RTMP/HLS/FLV）
        "total_sessions_lifetime": 1250,     // 流存活期间的累计会话数
        "current_active_sessions": 85,         // 当前仍在线的会话数
        "watermark_enabled": true,             // 是否启用水印（生成一刻就决定了）
        "protocol_summary": [                  // 按协议统计
          {
            "protocol_type": "RTMP",
            "total_sessions": 3000,
            "current_sessions": 30
          },
          {
            "protocol_type": "FLV",
            "total_sessions": 1450,
            "current_sessions": 5
          }
        ]
      }
      // 其他流...
    ]
  }
}

```

# 会话列表

```jsonc

//某流-会话列表
{
  "code": 0,
  "msg": "success",
  "data": {
    "stream_info": {                       // 流基础信息（同流列表字段）
      "stream_id": "stream12345",
      "stream_name": "XX摄像头流"
    },
    "protocol_list": [                     // 协议列表
      {
        "protocol_type": "RTMP",           // 协议类型：RTMP/HLS/FLV/DASH
        "status": "active",                // 协议状态：active/inactive
        "session_count": 30,               // 该协议下会话数
        "active_session_list": [
            {
              "session_id": "sess-9f3a1c8e",        // 全局唯一会话ID（强依赖）
              "client_id": "user-88721",             // 客户端身份（设备ID）
              "client_ip": "1.202.33.41",
              "client_type": "mobile",               // mobile / web / tv / sdk
              "tenant_id": "12145",                  // 拉流租户 湖南省生态环境厅
              "user_name": "张三",                   // 拉流用户 - 可能为空
              "protocol_type": "RTMP",                // RTMP / HLS / FLV / DASH
              "start_time": "2024-05-20 13:10:00", // 会话开始时间
              "play_duration": 1860,                 // 已播放时长（秒）
              "current_fps": 25,                     // fps
              "current_bitrate": 2500,               // kbps
              "current_resolution": "854x480",
              "node_id": "5",                      // 当前接入节点
              "instance_id": "inst-003",            // 当前处理实例
              "link_hops": [                        //  会话实际链路
                  {
                    "hop_index": 1,
                    "node_id": "origin-node", 
                    "latency_ms": 10
                  },
                  {
                    "hop_index": 2,
                    "node_id": "node-01", 
                    "latency_ms": 18
                  }
                ],
                "total_link_latency": 28,             //  链路总延迟
            }
         ]
      }
      // 其他协议...
    ]
  }
}
```

# `PostgreSQL`数据结构反推

## 收敛结论

本文基于上面的四类接口响应反推上报统计看板所需的数据结构。看板表按“上报读模型”设计，不按业务主库实体建模：`media_node`、`media_device_node`、`media_stream_alias`、`media_tenant_stream_config`等已有表仍负责配置，但看板查询不依赖这些配置表做实时装配。

建议只保留`5`张表：

| 表名 | 对应响应 | 是否快照 | 依赖策略 |
|------|----------|----------|----------|
| `media_report_node` | 总节点概览最新投影 | 否 | 节点名称、区域、父节点和延迟矩阵直接冗余 |
| `media_report_node_snapshot` | 总节点概览历史采样 | 是 | 仅服务总节点概览趋势和回放 |
| `media_report_instance` | 实例列表 | 否 | 冗余节点展示字段，不依赖节点配置表 |
| `media_report_stream` | 流信息列表 | 否 | 冗余源对象、节点、实例、租户字段；协议摘要放`JSONB` |
| `media_report_session` | 会话列表 | 否 | 冗余流、节点、实例、租户字段；链路跳点放`JSONB` |

## 设计边界

- 只有`总节点概览`需要历史快照，因此只保留`media_report_node_snapshot`；实例、流、会话只保存最新投影。
- 除`media_report_node_snapshot`外，不设计历史表或汇总表。
- 看板表不声明外键，`node_id`、`instance_id`、`stream_id`、`tenant_id`等只作为上报业务键和筛选维度使用。
- 页面展示需要的名称字段直接冗余，例如`node_name`、`instance_name`、`stream_name`，避免接口查询时回查配置表。
- 当前仅用于嵌套展示、且不参与列表筛选排序的数组结构使用`JSONB`，包括`node_latency_map`、`protocol_summary`和`link_hops`。
- 如果后续某个`JSONB`内部字段变成高频筛选、排序或排行条件，再补生成列、表达式索引或拆表，不在当前阶段提前拆。
- 数据权限过滤优先依赖各表冗余的`tenant_id`、`node_id`、`instance_id`等维度，在数据库查询阶段过滤，不先查全量再内存过滤。
- 本文只表达数据结构反推，不代表本次直接新增迁移。进入实现时应按插件 SQL 规范补充安装、卸载、幂等执行和 DAO 生成验证。

## 字段来源映射

| 响应区域 | 关键字段 | 建议主表 |
|----------|----------|----------|
| 总节点概览 | `node_id`、`node_name`、`region`、`parent_node_id`、`child_nodes` | `media_report_node` |
| 总节点概览 | `status`、`last_heartbeat`、`report_time` | `media_report_node`、`media_report_node_snapshot` |
| 总节点概览 | `total_nodes`、`alive_nodes`、`CPU`、内存、磁盘、网络、`live_streams`、`sessions`、`avg_delay` | 查询`media_report_node`、`media_report_instance`和`media_report_stream`后按节点子树聚合计算 |
| 节点延迟图 | `node_latency_map` | `media_report_node.node_latency_map`、`media_report_node_snapshot.node_latency_map` |
| 实例列表 | `instance_id`、`instance_name`、`version`、`start_time`、资源指标 | `media_report_instance` |
| 流信息列表 | `stream_id`、`source_url`、`resolution`、`fps`、`bitrate`、`packet_loss`、`watermark_enabled` | `media_report_stream` |
| 流协议统计 | `protocol_summary[].protocol_type`、`protocol_summary[].total_sessions` | `media_report_stream.protocol_summary` |
| 流协议统计 | `protocol_count`、`current_active_sessions`、`protocol_summary[].current_sessions` | 读取`media_report_session`后按`stream_id + protocol_type`聚合当前活跃会话数 |
| 会话列表 | `session_id`、`client_id`、`tenant_id`、`protocol_type`、播放质量字段 | `media_report_session`；`play_duration`缺省时由`start_time + report_time`计算 |
| 会话链路 | `link_hops`、`total_link_latency` | `media_report_session.link_hops`；`total_link_latency`优先由`link_hops[].latency_ms`求和 |

## 接口字段覆盖校验

| 接口区域 | 响应字段 | 数据来源 |
|----------|----------|----------|
| 总节点概览 | `node_id`、`node_name`、`region`、`status`、`parent_node_id`、`last_heartbeat`、`report_time`、`node_latency_map` | `media_report_node`同名字段 |
| 总节点概览 | `total_nodes`、`alive_nodes` | 查询`media_report_node`后按`parent_node_id`组装子树并统计节点数量与非离线节点数量 |
| 总节点概览 | `cpu_allocated`、`cpu_load`、`memory_allocated`、`memory_used`、`disk_io_read`、`disk_io_write`、`network_in`、`network_out`、`live_streams`、`sessions` | 按`node_id`聚合`media_report_instance`，父节点包含子树实例汇总；`live_streams`和`sessions`由服务端生命周期事件维护后写入实例投影 |
| 总节点概览 | `avg_delay` | 按`node_id`聚合`media_report_stream.avg_delay`，父节点包含子树流延迟平均值 |
| 总节点概览 | `child_nodes` | 查询`media_report_node`后按`parent_node_id`组装，子节点结构复用同一张表 |
| 实例列表 | `node_info.node_id`、`node_info.node_name`、`node_info.region`、`node_info.status` | 优先从`media_report_node`同名字段取当前查询节点信息；`media_report_instance`也冗余`node_id`、`node_name`、`region`、`node_status`用于非空列表快速返回 |
| 实例列表 | `instance_list` | 按`node_id`查询`media_report_instance`得到 |
| 实例列表 | `instance_id`、`instance_name`、`status`、`cpu_allocated`、`cpu_load`、`memory_allocated`、`memory_used`、`disk_io_read`、`disk_io_write`、`network_in`、`network_out`、`live_streams`、`sessions`、`start_time`、`version` | `media_report_instance`同名字段 |
| 流信息列表 | `source_type`、`source_id` | 请求条件或`media_report_stream`同名字段 |
| 流信息列表 | `stream_list` | 按`source_type`、`source_id`、`tenant_id`等条件查询`media_report_stream`得到 |
| 流信息列表 | `source_url`、`stream_id`、`stream_name`、`resolution`、`fps`、`bitrate`、`packet_loss`、`status`、`start_time`、`avg_delay`、`watermark_enabled` | `media_report_stream`同名字段 |
| 流信息列表 | `duration` | 优先取`media_report_stream.duration`；缺省时由`start_time + report_time`计算 |
| 流信息列表 | `protocol_count`、`total_sessions_lifetime` | 优先由`media_report_stream.protocol_summary`聚合计算；缺少协议摘要时回退到`media_report_stream`同名字段 |
| 流信息列表 | `current_active_sessions` | 读取`media_report_session`后按`stream_id`聚合当前会话数，避免依赖客户端直接上报实时值 |
| 流协议统计 | `protocol_summary[].protocol_type`、`protocol_summary[].total_sessions` | `media_report_stream.protocol_summary`内嵌`JSONB`字段 |
| 流协议统计 | `protocol_summary[].current_sessions` | 读取`media_report_session`后按`stream_id + protocol_type`聚合当前会话数 |
| 会话列表 | `stream_info.stream_id`、`stream_info.stream_name` | 优先从`media_report_stream`同名字段取当前查询流信息；`media_report_session`也冗余`stream_id`、`stream_name`用于非空列表快速返回 |
| 会话列表 | `protocol_list` | 读取`media_report_stream.protocol_summary`得到协议全集，再按`protocol_type`合并`media_report_session`会话明细 |
| 会话列表 | `protocol_list[].status` | 当前查询范围内`media_report_session`按协议计数大于`0`时返回`active`，否则返回`inactive` |
| 会话列表 | `protocol_list[].session_count` | 当前查询范围内按`media_report_session.protocol_type`聚合计数 |
| 会话列表 | `active_session_list[].session_id`、`client_id`、`client_ip`、`client_type`、`tenant_id`、`user_name`、`protocol_type`、`start_time`、`current_fps`、`current_bitrate`、`current_resolution`、`node_id`、`instance_id`、`link_hops` | `media_report_session`同名字段 |
| 会话列表 | `play_duration` | 优先取`media_report_session.play_duration`；缺省时由`start_time + report_time`计算 |
| 会话列表 | `total_link_latency` | 优先由`media_report_session.link_hops[].latency_ms`求和；缺少链路跳点时回退到`media_report_session.total_link_latency` |
| 会话链路 | `link_hops[].hop_index`、`link_hops[].node_id`、`link_hops[].latency_ms` | `media_report_session.link_hops`内嵌`JSONB`字段 |

## 建议`DDL`

以下`DDL`使用`COMMENT ON`为表和字段提供`PostgreSQL`备注。为了降低依赖，所有表都不声明外键；跨表字段均按上报时间点冗余保存。

### 总节点概览

```sql
CREATE TABLE IF NOT EXISTS media_report_node (
    "node_id" VARCHAR(64) PRIMARY KEY,
    "node_name" VARCHAR(128) NOT NULL DEFAULT '',
    "region" VARCHAR(128) NOT NULL DEFAULT '',
    "parent_node_id" VARCHAR(64) NOT NULL DEFAULT '0',
    "status" VARCHAR(32) NOT NULL DEFAULT '',
    "total_nodes" INTEGER NOT NULL DEFAULT 0,
    "alive_nodes" INTEGER NOT NULL DEFAULT 0,
    "cpu_allocated" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "cpu_load" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "memory_allocated" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "memory_used" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_read" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_write" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_in" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_out" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "live_streams" INTEGER NOT NULL DEFAULT 0,
    "sessions" INTEGER NOT NULL DEFAULT 0,
    "avg_delay" INTEGER NOT NULL DEFAULT 0,
    "last_heartbeat" TIMESTAMP,
    "node_latency_map" JSONB NOT NULL DEFAULT '{}'::JSONB,
    "report_time" BIGINT NOT NULL,
    "updated_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_media_report_node_parent
    ON media_report_node ("parent_node_id");

CREATE INDEX IF NOT EXISTS idx_media_report_node_status
    ON media_report_node ("status");

CREATE INDEX IF NOT EXISTS idx_media_report_node_report_time
    ON media_report_node ("report_time" DESC);

COMMENT ON TABLE media_report_node IS '媒体看板节点总览表，保存节点概览最新上报投影';
COMMENT ON COLUMN media_report_node."node_id" IS '上报节点业务标识，不依赖配置表外键';
COMMENT ON COLUMN media_report_node."node_name" IS '节点展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_node."region" IS '节点所属区域或机房名称';
COMMENT ON COLUMN media_report_node."parent_node_id" IS '父节点业务标识，根节点使用0';
COMMENT ON COLUMN media_report_node."status" IS '节点运行状态，保存上报原始枚举值';
COMMENT ON COLUMN media_report_node."total_nodes" IS '当前节点视角下的节点总数';
COMMENT ON COLUMN media_report_node."alive_nodes" IS '当前节点视角下的在线节点数';
COMMENT ON COLUMN media_report_node."cpu_allocated" IS '已分配CPU容量，单位由上报端统一';
COMMENT ON COLUMN media_report_node."cpu_load" IS 'CPU负载或利用率，单位由上报端统一';
COMMENT ON COLUMN media_report_node."memory_allocated" IS '已分配内存容量，单位GB';
COMMENT ON COLUMN media_report_node."memory_used" IS '已使用内存容量，单位GB';
COMMENT ON COLUMN media_report_node."disk_io_read" IS '磁盘读取速率，单位MB/s';
COMMENT ON COLUMN media_report_node."disk_io_write" IS '磁盘写入速率，单位MB/s';
COMMENT ON COLUMN media_report_node."network_in" IS '网络入站速率，单位Mbps';
COMMENT ON COLUMN media_report_node."network_out" IS '网络出站速率，单位Mbps';
COMMENT ON COLUMN media_report_node."live_streams" IS '当前直播流数量';
COMMENT ON COLUMN media_report_node."sessions" IS '当前会话数量';
COMMENT ON COLUMN media_report_node."avg_delay" IS '节点平均延迟，单位毫秒';
COMMENT ON COLUMN media_report_node."last_heartbeat" IS '节点最后心跳时间';
COMMENT ON COLUMN media_report_node."node_latency_map" IS '节点到其他节点的延迟矩阵JSON，结构来自接口node_latency_map';
COMMENT ON COLUMN media_report_node."report_time" IS '上报端采样时间';
COMMENT ON COLUMN media_report_node."updated_at" IS '记录更新时间';
```

### 总节点概览快照

```sql
CREATE TABLE IF NOT EXISTS media_report_node_snapshot (
    "node_id" VARCHAR(64) NOT NULL,
    "report_time" BIGINT NOT NULL,
    "node_name" VARCHAR(128) NOT NULL DEFAULT '',
    "region" VARCHAR(128) NOT NULL DEFAULT '',
    "parent_node_id" VARCHAR(64) NOT NULL DEFAULT '0',
    "status" VARCHAR(32) NOT NULL DEFAULT '',
    "total_nodes" INTEGER NOT NULL DEFAULT 0,
    "alive_nodes" INTEGER NOT NULL DEFAULT 0,
    "cpu_allocated" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "cpu_load" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "memory_allocated" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "memory_used" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_read" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_write" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_in" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_out" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "live_streams" INTEGER NOT NULL DEFAULT 0,
    "sessions" INTEGER NOT NULL DEFAULT 0,
    "avg_delay" INTEGER NOT NULL DEFAULT 0,
    "last_heartbeat" TIMESTAMP,
    "node_latency_map" JSONB NOT NULL DEFAULT '{}'::JSONB,
    PRIMARY KEY ("node_id", "report_time")
);

CREATE INDEX IF NOT EXISTS idx_media_report_node_snapshot_time
    ON media_report_node_snapshot ("report_time" DESC);

CREATE INDEX IF NOT EXISTS idx_media_report_node_snapshot_parent_time
    ON media_report_node_snapshot ("parent_node_id", "report_time" DESC);

COMMENT ON TABLE media_report_node_snapshot IS '媒体看板节点总览快照表，仅保存总节点概览需要的历史采样';
COMMENT ON COLUMN media_report_node_snapshot."node_id" IS '上报节点业务标识，不依赖配置表外键';
COMMENT ON COLUMN media_report_node_snapshot."report_time" IS '上报端采样时间，也是快照时间';
COMMENT ON COLUMN media_report_node_snapshot."node_name" IS '节点展示名称，按快照时间点冗余';
COMMENT ON COLUMN media_report_node_snapshot."region" IS '节点所属区域或机房名称';
COMMENT ON COLUMN media_report_node_snapshot."parent_node_id" IS '父节点业务标识，根节点使用0';
COMMENT ON COLUMN media_report_node_snapshot."status" IS '节点运行状态，保存上报原始枚举值';
COMMENT ON COLUMN media_report_node_snapshot."total_nodes" IS '快照时节点视角下的节点总数';
COMMENT ON COLUMN media_report_node_snapshot."alive_nodes" IS '快照时节点视角下的在线节点数';
COMMENT ON COLUMN media_report_node_snapshot."cpu_allocated" IS '快照时已分配CPU容量，单位由上报端统一';
COMMENT ON COLUMN media_report_node_snapshot."cpu_load" IS '快照时CPU负载或利用率，单位由上报端统一';
COMMENT ON COLUMN media_report_node_snapshot."memory_allocated" IS '快照时已分配内存容量，单位GB';
COMMENT ON COLUMN media_report_node_snapshot."memory_used" IS '快照时已使用内存容量，单位GB';
COMMENT ON COLUMN media_report_node_snapshot."disk_io_read" IS '快照时磁盘读取速率，单位MB/s';
COMMENT ON COLUMN media_report_node_snapshot."disk_io_write" IS '快照时磁盘写入速率，单位MB/s';
COMMENT ON COLUMN media_report_node_snapshot."network_in" IS '快照时网络入站速率，单位Mbps';
COMMENT ON COLUMN media_report_node_snapshot."network_out" IS '快照时网络出站速率，单位Mbps';
COMMENT ON COLUMN media_report_node_snapshot."live_streams" IS '快照时直播流数量';
COMMENT ON COLUMN media_report_node_snapshot."sessions" IS '快照时会话数量';
COMMENT ON COLUMN media_report_node_snapshot."avg_delay" IS '快照时节点平均延迟，单位毫秒';
COMMENT ON COLUMN media_report_node_snapshot."last_heartbeat" IS '快照时节点最后心跳时间';
COMMENT ON COLUMN media_report_node_snapshot."node_latency_map" IS '快照时节点延迟矩阵JSON，结构来自接口node_latency_map';
```

### 实例列表

```sql
CREATE TABLE IF NOT EXISTS media_report_instance (
    "instance_id" VARCHAR(128) PRIMARY KEY,
    "instance_name" VARCHAR(128) NOT NULL DEFAULT '',
    "node_id" VARCHAR(64) NOT NULL DEFAULT '',
    "node_name" VARCHAR(128) NOT NULL DEFAULT '',
    "region" VARCHAR(128) NOT NULL DEFAULT '',
    "node_status" VARCHAR(32) NOT NULL DEFAULT '',
    "status" VARCHAR(32) NOT NULL DEFAULT '',
    "cpu_allocated" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "cpu_load" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "memory_allocated" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "memory_used" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_read" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "disk_io_write" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_in" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "network_out" NUMERIC(14,2) NOT NULL DEFAULT 0,
    "live_streams" INTEGER NOT NULL DEFAULT 0,
    "sessions" INTEGER NOT NULL DEFAULT 0,
    "start_time" TIMESTAMP,
    "version" VARCHAR(64) NOT NULL DEFAULT '',
    "report_time" BIGINT NOT NULL,
    "updated_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_media_report_instance_node
    ON media_report_instance ("node_id", "status");

CREATE INDEX IF NOT EXISTS idx_media_report_instance_status
    ON media_report_instance ("status");

COMMENT ON TABLE media_report_instance IS '媒体看板实例表，保存实例列表最新上报投影';
COMMENT ON COLUMN media_report_instance."instance_id" IS '实例业务标识，不依赖实例配置外键';
COMMENT ON COLUMN media_report_instance."instance_name" IS '实例展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_instance."node_id" IS '实例所属节点业务标识';
COMMENT ON COLUMN media_report_instance."node_name" IS '实例所属节点展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_instance."region" IS '实例所属区域或机房名称';
COMMENT ON COLUMN media_report_instance."node_status" IS '实例所属节点状态，用于返回node_info.status';
COMMENT ON COLUMN media_report_instance."status" IS '实例运行状态，保存上报原始枚举值';
COMMENT ON COLUMN media_report_instance."cpu_allocated" IS '实例已分配CPU容量，单位由上报端统一';
COMMENT ON COLUMN media_report_instance."cpu_load" IS '实例CPU负载或利用率，单位由上报端统一';
COMMENT ON COLUMN media_report_instance."memory_allocated" IS '实例已分配内存容量，单位GB';
COMMENT ON COLUMN media_report_instance."memory_used" IS '实例已使用内存容量，单位GB';
COMMENT ON COLUMN media_report_instance."disk_io_read" IS '实例磁盘读取速率，单位MB/s';
COMMENT ON COLUMN media_report_instance."disk_io_write" IS '实例磁盘写入速率，单位MB/s';
COMMENT ON COLUMN media_report_instance."network_in" IS '实例网络入站速率，单位Mbps';
COMMENT ON COLUMN media_report_instance."network_out" IS '实例网络出站速率，单位Mbps';
COMMENT ON COLUMN media_report_instance."live_streams" IS '实例当前承载直播流数量';
COMMENT ON COLUMN media_report_instance."sessions" IS '实例当前会话数量';
COMMENT ON COLUMN media_report_instance."start_time" IS '实例启动时间';
COMMENT ON COLUMN media_report_instance."version" IS '实例版本号';
COMMENT ON COLUMN media_report_instance."report_time" IS '上报端采样时间';
COMMENT ON COLUMN media_report_instance."updated_at" IS '记录更新时间';
```

### 流信息列表

```sql
CREATE TABLE IF NOT EXISTS media_report_stream (
    "stream_id" VARCHAR(128) PRIMARY KEY,
    "source_type" VARCHAR(32) NOT NULL DEFAULT '',
    "source_id" VARCHAR(128) NOT NULL DEFAULT '',
    "tenant_id" VARCHAR(64) NOT NULL DEFAULT '',
    "node_id" VARCHAR(64) NOT NULL DEFAULT '',
    "node_name" VARCHAR(128) NOT NULL DEFAULT '',
    "instance_id" VARCHAR(128) NOT NULL DEFAULT '',
    "instance_name" VARCHAR(128) NOT NULL DEFAULT '',
    "source_url" VARCHAR(1024) NOT NULL DEFAULT '',
    "stream_name" VARCHAR(255) NOT NULL DEFAULT '',
    "resolution" VARCHAR(32) NOT NULL DEFAULT '',
    "fps" NUMERIC(8,2) NOT NULL DEFAULT 0,
    "bitrate" INTEGER NOT NULL DEFAULT 0,
    "packet_loss" NUMERIC(8,4) NOT NULL DEFAULT 0,
    "status" VARCHAR(32) NOT NULL DEFAULT '',
    "start_time" TIMESTAMP,
    "duration" INTEGER NOT NULL DEFAULT 0,
    "avg_delay" INTEGER NOT NULL DEFAULT 0,
    "protocol_count" INTEGER NOT NULL DEFAULT 0,
    "total_sessions_lifetime" BIGINT NOT NULL DEFAULT 0,
    "current_active_sessions" INTEGER NOT NULL DEFAULT 0,
    "watermark_enabled" BOOLEAN NOT NULL DEFAULT FALSE,
    "protocol_summary" JSONB NOT NULL DEFAULT '[]'::JSONB,
    "report_time" BIGINT NOT NULL,
    "updated_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_media_report_stream_source
    ON media_report_stream ("source_type", "source_id", "status");

CREATE INDEX IF NOT EXISTS idx_media_report_stream_tenant
    ON media_report_stream ("tenant_id", "status");

CREATE INDEX IF NOT EXISTS idx_media_report_stream_node_instance
    ON media_report_stream ("node_id", "instance_id");

COMMENT ON TABLE media_report_stream IS '媒体看板流表，保存流列表和协议摘要的最新上报投影';
COMMENT ON COLUMN media_report_stream."stream_id" IS '流业务标识，不依赖流配置外键';
COMMENT ON COLUMN media_report_stream."source_type" IS '来源类型，例如节点或实例';
COMMENT ON COLUMN media_report_stream."source_id" IS '来源业务标识，用于节点或实例下钻';
COMMENT ON COLUMN media_report_stream."tenant_id" IS '租户标识，用于数据权限过滤和租户统计';
COMMENT ON COLUMN media_report_stream."node_id" IS '流所属节点业务标识';
COMMENT ON COLUMN media_report_stream."node_name" IS '流所属节点展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_stream."instance_id" IS '流所属实例业务标识';
COMMENT ON COLUMN media_report_stream."instance_name" IS '流所属实例展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_stream."source_url" IS '流源地址';
COMMENT ON COLUMN media_report_stream."stream_name" IS '流展示名称';
COMMENT ON COLUMN media_report_stream."resolution" IS '当前分辨率';
COMMENT ON COLUMN media_report_stream."fps" IS '当前帧率';
COMMENT ON COLUMN media_report_stream."bitrate" IS '当前码率，单位Kbps';
COMMENT ON COLUMN media_report_stream."packet_loss" IS '当前丢包率';
COMMENT ON COLUMN media_report_stream."status" IS '流运行状态，保存上报原始枚举值';
COMMENT ON COLUMN media_report_stream."start_time" IS '流开始时间';
COMMENT ON COLUMN media_report_stream."duration" IS '流持续时间，单位秒';
COMMENT ON COLUMN media_report_stream."avg_delay" IS '流平均延迟，单位毫秒';
COMMENT ON COLUMN media_report_stream."protocol_count" IS '支持的协议数量';
COMMENT ON COLUMN media_report_stream."total_sessions_lifetime" IS '流历史累计会话数量';
COMMENT ON COLUMN media_report_stream."current_active_sessions" IS '流当前活跃会话数量';
COMMENT ON COLUMN media_report_stream."watermark_enabled" IS '当前是否启用水印';
COMMENT ON COLUMN media_report_stream."protocol_summary" IS '协议摘要JSON数组，结构来自接口protocol_summary';
COMMENT ON COLUMN media_report_stream."report_time" IS '上报端采样时间';
COMMENT ON COLUMN media_report_stream."updated_at" IS '记录更新时间';
```

### 会话列表

```sql
CREATE TABLE IF NOT EXISTS media_report_session (
    "session_id" VARCHAR(128) PRIMARY KEY,
    "stream_id" VARCHAR(128) NOT NULL DEFAULT '',
    "stream_name" VARCHAR(255) NOT NULL DEFAULT '',
    "tenant_id" VARCHAR(64) NOT NULL DEFAULT '',
    "client_id" VARCHAR(128) NOT NULL DEFAULT '',
    "client_ip" INET,
    "client_type" VARCHAR(32) NOT NULL DEFAULT '',
    "user_name" VARCHAR(128) NOT NULL DEFAULT '',
    "protocol_type" VARCHAR(32) NOT NULL DEFAULT '',
    "start_time" TIMESTAMP,
    "play_duration" INTEGER NOT NULL DEFAULT 0,
    "current_fps" NUMERIC(8,2) NOT NULL DEFAULT 0,
    "current_bitrate" INTEGER NOT NULL DEFAULT 0,
    "current_resolution" VARCHAR(32) NOT NULL DEFAULT '',
    "node_id" VARCHAR(64) NOT NULL DEFAULT '',
    "node_name" VARCHAR(128) NOT NULL DEFAULT '',
    "instance_id" VARCHAR(128) NOT NULL DEFAULT '',
    "instance_name" VARCHAR(128) NOT NULL DEFAULT '',
    "link_hops" JSONB NOT NULL DEFAULT '[]'::JSONB,
    "total_link_latency" INTEGER NOT NULL DEFAULT 0,
    "report_time" BIGINT NOT NULL,
    "updated_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_media_report_session_stream
    ON media_report_session ("stream_id", "protocol_type");

CREATE INDEX IF NOT EXISTS idx_media_report_session_tenant
    ON media_report_session ("tenant_id", "protocol_type");

CREATE INDEX IF NOT EXISTS idx_media_report_session_node_instance
    ON media_report_session ("node_id", "instance_id");

COMMENT ON TABLE media_report_session IS '媒体看板会话表，保存会话列表和链路跳点的最新上报投影';
COMMENT ON COLUMN media_report_session."session_id" IS '会话业务标识';
COMMENT ON COLUMN media_report_session."stream_id" IS '会话所属流业务标识';
COMMENT ON COLUMN media_report_session."stream_name" IS '会话所属流展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_session."tenant_id" IS '租户标识，用于数据权限过滤和租户统计';
COMMENT ON COLUMN media_report_session."client_id" IS '客户端标识';
COMMENT ON COLUMN media_report_session."client_ip" IS '客户端IP地址';
COMMENT ON COLUMN media_report_session."client_type" IS '客户端类型';
COMMENT ON COLUMN media_report_session."user_name" IS '播放用户展示名称';
COMMENT ON COLUMN media_report_session."protocol_type" IS '播放协议类型';
COMMENT ON COLUMN media_report_session."start_time" IS '会话开始时间';
COMMENT ON COLUMN media_report_session."play_duration" IS '播放持续时间，单位秒';
COMMENT ON COLUMN media_report_session."current_fps" IS '当前播放帧率';
COMMENT ON COLUMN media_report_session."current_bitrate" IS '当前播放码率，单位Kbps';
COMMENT ON COLUMN media_report_session."current_resolution" IS '当前播放分辨率';
COMMENT ON COLUMN media_report_session."node_id" IS '会话承载节点业务标识';
COMMENT ON COLUMN media_report_session."node_name" IS '会话承载节点展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_session."instance_id" IS '会话承载实例业务标识';
COMMENT ON COLUMN media_report_session."instance_name" IS '会话承载实例展示名称，按上报时间点冗余';
COMMENT ON COLUMN media_report_session."link_hops" IS '会话链路跳点JSON数组，结构来自接口link_hops';
COMMENT ON COLUMN media_report_session."total_link_latency" IS '会话链路总延迟，单位毫秒';
COMMENT ON COLUMN media_report_session."report_time" IS '上报端采样时间';
COMMENT ON COLUMN media_report_session."updated_at" IS '记录更新时间';
```

## 查询装配建议

- 总节点概览：读取`media_report_node`，按`parent_node_id`在内存中组装`child_nodes`；历史趋势或回放只查`media_report_node_snapshot`。
- 实例列表：按`node_id`、`status`等条件查询`media_report_instance`，不回查节点配置表。
- 流信息列表：按`source_type + source_id`、`tenant_id`或`status`查询`media_report_stream`，协议统计直接读取`protocol_summary`。
- 会话列表：按`stream_id`、`tenant_id`、`protocol_type`、`node_id`或`instance_id`查询`media_report_session`，链路详情直接读取`link_hops`。

## 维护策略

- 最新投影表使用上报业务键做`UPSERT`，例如节点用`node_id`、实例用`instance_id`、流用`stream_id`、会话用`session_id`。
- 总节点概览快照使用`node_id + report_time`作为幂等键，避免重复写入同一采样点。
- 上报写入失败时可以重放同一批数据；最新投影覆盖更新，快照按主键去重。
- `updated_at`由写入侧或`GoFrame`自动时间维护，不作为上报载荷字段。
- 这些看板读表不包含`deleted_at`；最新投影记录按上报结果覆盖或过期硬清理，快照按保留周期清理。
- 不新增缓存表或运行时缓存；若前端需要更低延迟，可在服务层对最新投影查询增加短`TTL`缓存，并以这些表作为权威数据源。
