# Mail - SMTP

`linapro-mail-smtp` sends email through a standard SMTP server for `linapro-mail-core`.

- Registers `kind=smtp` outbound SPI
- Does not own Connection/Account tables
- Depends on `linapro-mail-core`
