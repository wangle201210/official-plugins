# 配置说明

当前版本没有额外运行时设置。插件启用后，按`plugin.yaml`声明的入口使用即可。

## 说明

- 在`智能中心`页面维护渠道、端点、模型身份和能力档位。
- 供应商密钥只通过插件设置流程写入；保留已有脱敏密钥时保持字段为空。

## 入口

| 名称 | 路径 |
| --- | --- |
| 渠道管理 | `/ai/providers` |
| 模型管理 | `/ai/models` |
| 档位管理 | `/ai/tiers` |
| 调用日志 | `/ai/invocations` |
