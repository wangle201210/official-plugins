# Water Source Plugin

The water source plugin provides image watermark processing based on the shared media strategy tables.

It reads `media_strategy`, `media_strategy_tenant`, `media_strategy_device`, and `media_strategy_device_tenant`; it does not create legacy `hg_*` tables or tenant-isolated storage.
